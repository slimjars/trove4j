dependencies {
    compile project(':trove4j-float-array-list')
    compile project(':trove4j-float-stack')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: float-array-stack'
                }
            }
        }
    }
}
