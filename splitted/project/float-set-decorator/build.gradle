dependencies {
    compile project(':trove4j-float-set')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: float-set-decorator'
                }
            }
        }
    }
}
