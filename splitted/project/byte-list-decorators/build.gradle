dependencies {
    compile project(':trove4j-byte-list-decorator')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: byte-list-decorators'
                }
            }
        }
    }
}
