dependencies {
    compile project(':trove4j-synchronized-char-int-map')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: synchronized-char-int-maps'
                }
            }
        }
    }
}
