dependencies {
    compile project(':trove4j-unmodifiable-byte-float-map')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: unmodifiable-byte-float-maps'
                }
            }
        }
    }
}
