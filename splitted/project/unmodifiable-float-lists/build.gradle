dependencies {
    compile project(':trove4j-unmodifiable-float-list')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: unmodifiable-float-lists'
                }
            }
        }
    }
}
