dependencies {
    compile project(':trove4j-synchronized-object-long-map')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: synchronized-object-long-maps'
                }
            }
        }
    }
}
