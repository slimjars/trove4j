dependencies {
    compile project(':trove4j-synchronized-short-object-map')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: synchronized-short-object-maps'
                }
            }
        }
    }
}
