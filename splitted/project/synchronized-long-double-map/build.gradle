dependencies {
    compile project(':trove4j-long-double-map')
    compile project(':trove4j-synchronized-double-collection')
    compile project(':trove4j-synchronized-long-set')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: synchronized-long-double-map'
                }
            }
        }
    }
}
