dependencies {
    compile project(':trove4j-synchronized-int-short-map')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: synchronized-int-short-maps'
                }
            }
        }
    }
}
