dependencies {
    compile project(':trove4j-byte-short-map-decorator')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: byte-short-map-decorators'
                }
            }
        }
    }
}
