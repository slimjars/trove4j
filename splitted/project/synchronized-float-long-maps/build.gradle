dependencies {
    compile project(':trove4j-synchronized-float-long-map')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: synchronized-float-long-maps'
                }
            }
        }
    }
}
