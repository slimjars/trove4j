dependencies {
    compile project(':trove4j-double-collection')
    compile project(':trove4j-double-function')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: double-list'
                }
            }
        }
    }
}
