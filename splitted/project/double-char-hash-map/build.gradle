dependencies {
    compile project(':trove4j-double-char-hash')
    compile project(':trove4j-double-char-map')
    compile project(':trove4j-hash-primitive-iterator')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: double-char-hash-map'
                }
            }
        }
    }
}
