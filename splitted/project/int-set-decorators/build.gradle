dependencies {
    compile project(':trove4j-int-set-decorator')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: int-set-decorators'
                }
            }
        }
    }
}
