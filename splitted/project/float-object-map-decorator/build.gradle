dependencies {
    compile project(':trove4j-float-object-map')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: float-object-map-decorator'
                }
            }
        }
    }
}
