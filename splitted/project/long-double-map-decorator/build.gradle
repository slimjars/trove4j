dependencies {
    compile project(':trove4j-long-double-map')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: long-double-map-decorator'
                }
            }
        }
    }
}
