dependencies {
    compile project(':trove4j-int-short-map')
    compile project(':trove4j-synchronized-int-set')
    compile project(':trove4j-synchronized-short-collection')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: synchronized-int-short-map'
                }
            }
        }
    }
}
