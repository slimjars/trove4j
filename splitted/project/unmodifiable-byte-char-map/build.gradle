dependencies {
    compile project(':trove4j-byte-char-map')
    compile project(':trove4j-unmodifiable-byte-sets')
    compile project(':trove4j-unmodifiable-char-collections')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: unmodifiable-byte-char-map'
                }
            }
        }
    }
}
