dependencies {
    compile project(':trove4j-byte-int-map-decorator')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: byte-int-map-decorators'
                }
            }
        }
    }
}
