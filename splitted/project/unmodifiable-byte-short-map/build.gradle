dependencies {
    compile project(':trove4j-byte-short-map')
    compile project(':trove4j-unmodifiable-byte-sets')
    compile project(':trove4j-unmodifiable-short-collections')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: unmodifiable-byte-short-map'
                }
            }
        }
    }
}
