dependencies {
    compile project(':trove4j-long-double-map')
    compile project(':trove4j-unmodifiable-double-collections')
    compile project(':trove4j-unmodifiable-long-sets')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: unmodifiable-long-double-map'
                }
            }
        }
    }
}
