dependencies {
    compile project(':trove4j-byte-byte-map')
    compile project(':trove4j-synchronized-byte-set')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: synchronized-byte-byte-map'
                }
            }
        }
    }
}
