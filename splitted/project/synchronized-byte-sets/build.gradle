dependencies {
    compile project(':trove4j-synchronized-byte-set')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: synchronized-byte-sets'
                }
            }
        }
    }
}
