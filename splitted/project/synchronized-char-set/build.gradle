dependencies {
    compile project(':trove4j-char-set')
    compile project(':trove4j-synchronized-char-collection')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: synchronized-char-set'
                }
            }
        }
    }
}
