dependencies {
    compile project(':trove4j-long-object-map')
    compile project(':trove4j-synchronized-collection')
    compile project(':trove4j-synchronized-long-set')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: synchronized-long-object-map'
                }
            }
        }
    }
}
