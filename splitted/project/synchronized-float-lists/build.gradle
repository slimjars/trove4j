dependencies {
    compile project(':trove4j-synchronized-float-list')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: synchronized-float-lists'
                }
            }
        }
    }
}
