dependencies {
    compile project(':trove4j-synchronized-byte-int-map')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: synchronized-byte-int-maps'
                }
            }
        }
    }
}
