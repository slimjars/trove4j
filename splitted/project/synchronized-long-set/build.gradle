dependencies {
    compile project(':trove4j-long-set')
    compile project(':trove4j-synchronized-long-collection')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: synchronized-long-set'
                }
            }
        }
    }
}
