dependencies {
    compile project(':trove4j-unmodifiable-object-float-map')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: unmodifiable-object-float-maps'
                }
            }
        }
    }
}
