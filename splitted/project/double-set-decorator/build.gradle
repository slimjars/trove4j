dependencies {
    compile project(':trove4j-double-set')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: double-set-decorator'
                }
            }
        }
    }
}
