dependencies {
    compile project(':trove4j-synchronized-short-double-map')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: synchronized-short-double-maps'
                }
            }
        }
    }
}
