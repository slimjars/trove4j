dependencies {
    compile project(':trove4j-synchronized-int-char-map')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: synchronized-int-char-maps'
                }
            }
        }
    }
}
