dependencies {
    compile project(':trove4j-synchronized-object-char-map')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: synchronized-object-char-maps'
                }
            }
        }
    }
}
