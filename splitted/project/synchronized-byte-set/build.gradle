dependencies {
    compile project(':trove4j-byte-set')
    compile project(':trove4j-synchronized-byte-collection')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: synchronized-byte-set'
                }
            }
        }
    }
}
