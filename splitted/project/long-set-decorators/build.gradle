dependencies {
    compile project(':trove4j-long-set-decorator')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: long-set-decorators'
                }
            }
        }
    }
}
