dependencies {
    compile project(':trove4j-unmodifiable-byte-int-map')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: unmodifiable-byte-int-maps'
                }
            }
        }
    }
}
