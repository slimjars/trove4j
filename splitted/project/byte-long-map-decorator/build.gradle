dependencies {
    compile project(':trove4j-byte-long-map')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: byte-long-map-decorator'
                }
            }
        }
    }
}
