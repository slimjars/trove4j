dependencies {
    compile project(':trove4j-float-collection')
    compile project(':trove4j-float-function')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: float-list'
                }
            }
        }
    }
}
