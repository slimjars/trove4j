dependencies {
    compile project(':trove4j-char-short-map')
    compile project(':trove4j-synchronized-char-set')
    compile project(':trove4j-synchronized-short-collection')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: synchronized-char-short-map'
                }
            }
        }
    }
}
