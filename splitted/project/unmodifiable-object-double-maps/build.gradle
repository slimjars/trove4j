dependencies {
    compile project(':trove4j-unmodifiable-object-double-map')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: unmodifiable-object-double-maps'
                }
            }
        }
    }
}
