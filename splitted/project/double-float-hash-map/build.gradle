dependencies {
    compile project(':trove4j-double-float-hash')
    compile project(':trove4j-double-float-map')
    compile project(':trove4j-hash-primitive-iterator')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: double-float-hash-map'
                }
            }
        }
    }
}
