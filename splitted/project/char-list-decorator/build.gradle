dependencies {
    compile project(':trove4j-char-list')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: char-list-decorator'
                }
            }
        }
    }
}
