dependencies {
    compile project(':trove4j-byte-collection')
    compile project(':trove4j-byte-function')
    compile project(':trove4j-long-byte-iterator')
    compile project(':trove4j-long-byte-procedure')
    compile project(':trove4j-long-set')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: long-byte-map'
                }
            }
        }
    }
}
