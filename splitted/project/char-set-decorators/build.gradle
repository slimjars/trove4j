dependencies {
    compile project(':trove4j-char-set-decorator')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: char-set-decorators'
                }
            }
        }
    }
}
