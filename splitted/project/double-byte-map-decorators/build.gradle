dependencies {
    compile project(':trove4j-double-byte-map-decorator')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: double-byte-map-decorators'
                }
            }
        }
    }
}
