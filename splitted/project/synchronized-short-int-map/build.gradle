dependencies {
    compile project(':trove4j-short-int-map')
    compile project(':trove4j-synchronized-int-collection')
    compile project(':trove4j-synchronized-short-set')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: synchronized-short-int-map'
                }
            }
        }
    }
}
