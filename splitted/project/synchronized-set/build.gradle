dependencies {
    compile project(':trove4j-synchronized-collection')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: synchronized-set'
                }
            }
        }
    }
}
