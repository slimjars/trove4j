dependencies {
    compile project(':trove4j-hash-iterator')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: object-hash-iterator'
                }
            }
        }
    }
}
