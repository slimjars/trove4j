dependencies {
    compile project(':trove4j-synchronized-float-float-map')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: synchronized-float-float-maps'
                }
            }
        }
    }
}
