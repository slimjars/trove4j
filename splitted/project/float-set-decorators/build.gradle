dependencies {
    compile project(':trove4j-float-set-decorator')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: float-set-decorators'
                }
            }
        }
    }
}
