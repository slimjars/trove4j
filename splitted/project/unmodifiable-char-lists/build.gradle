dependencies {
    compile project(':trove4j-unmodifiable-char-list')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: unmodifiable-char-lists'
                }
            }
        }
    }
}
