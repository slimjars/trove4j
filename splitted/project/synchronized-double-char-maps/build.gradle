dependencies {
    compile project(':trove4j-synchronized-double-char-map')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: synchronized-double-char-maps'
                }
            }
        }
    }
}
