dependencies {
    compile project(':trove4j-byte-float-map')
    compile project(':trove4j-synchronized-byte-set')
    compile project(':trove4j-synchronized-float-collection')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: synchronized-byte-float-map'
                }
            }
        }
    }
}
