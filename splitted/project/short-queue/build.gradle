dependencies {
    compile project(':trove4j-short-collection')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: short-queue'
                }
            }
        }
    }
}
