dependencies {
    compile project(':trove4j-double-float-map')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: double-float-map-decorator'
                }
            }
        }
    }
}
