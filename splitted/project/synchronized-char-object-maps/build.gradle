dependencies {
    compile project(':trove4j-synchronized-char-object-map')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: synchronized-char-object-maps'
                }
            }
        }
    }
}
