dependencies {
    compile project(':trove4j-double-char-map')
    compile project(':trove4j-synchronized-char-collection')
    compile project(':trove4j-synchronized-double-set')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: synchronized-double-char-map'
                }
            }
        }
    }
}
