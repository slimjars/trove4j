dependencies {
    compile project(':trove4j-object-short-map')
    compile project(':trove4j-unmodifiable-short-collections')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: unmodifiable-object-short-map'
                }
            }
        }
    }
}
