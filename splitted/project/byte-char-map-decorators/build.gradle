dependencies {
    compile project(':trove4j-byte-char-map-decorator')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: byte-char-map-decorators'
                }
            }
        }
    }
}
