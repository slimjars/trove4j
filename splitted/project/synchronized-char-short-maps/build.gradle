dependencies {
    compile project(':trove4j-synchronized-char-short-map')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: synchronized-char-short-maps'
                }
            }
        }
    }
}
