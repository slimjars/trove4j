dependencies {
    compile project(':trove4j-byte-char-hash')
    compile project(':trove4j-byte-char-map')
    compile project(':trove4j-hash-primitive-iterator')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: byte-char-hash-map'
                }
            }
        }
    }
}
