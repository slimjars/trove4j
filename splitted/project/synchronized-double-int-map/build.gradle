dependencies {
    compile project(':trove4j-double-int-map')
    compile project(':trove4j-synchronized-double-set')
    compile project(':trove4j-synchronized-int-collection')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: synchronized-double-int-map'
                }
            }
        }
    }
}
