dependencies {
    compile project(':trove4j-double-int-map')
    compile project(':trove4j-unmodifiable-double-sets')
    compile project(':trove4j-unmodifiable-int-collections')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: unmodifiable-double-int-map'
                }
            }
        }
    }
}
