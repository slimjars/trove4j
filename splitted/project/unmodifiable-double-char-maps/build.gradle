dependencies {
    compile project(':trove4j-unmodifiable-double-char-map')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: unmodifiable-double-char-maps'
                }
            }
        }
    }
}
