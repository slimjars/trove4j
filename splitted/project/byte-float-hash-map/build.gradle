dependencies {
    compile project(':trove4j-byte-float-hash')
    compile project(':trove4j-byte-float-map')
    compile project(':trove4j-hash-primitive-iterator')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: byte-float-hash-map'
                }
            }
        }
    }
}
