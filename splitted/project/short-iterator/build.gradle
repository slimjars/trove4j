dependencies {
    compile project(':trove4j-iterator')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: short-iterator'
                }
            }
        }
    }
}
