dependencies {
    compile project(':trove4j-synchronized-object-float-map')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: synchronized-object-float-maps'
                }
            }
        }
    }
}
