dependencies {
    compile project(':trove4j-byte-long-hash')
    compile project(':trove4j-byte-long-map')
    compile project(':trove4j-hash-primitive-iterator')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: byte-long-hash-map'
                }
            }
        }
    }
}
