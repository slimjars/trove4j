dependencies {
    compile project(':trove4j-synchronized-double-byte-map')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: synchronized-double-byte-maps'
                }
            }
        }
    }
}
