dependencies {
    compile project(':trove4j-float-object-map')
    compile project(':trove4j-synchronized-collection')
    compile project(':trove4j-synchronized-float-set')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: synchronized-float-object-map'
                }
            }
        }
    }
}
