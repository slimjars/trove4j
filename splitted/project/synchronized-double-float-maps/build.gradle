dependencies {
    compile project(':trove4j-synchronized-double-float-map')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: synchronized-double-float-maps'
                }
            }
        }
    }
}
