dependencies {
    compile project(':trove4j-byte-object-map')
    compile project(':trove4j-unmodifiable-byte-sets')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: unmodifiable-byte-object-map'
                }
            }
        }
    }
}
