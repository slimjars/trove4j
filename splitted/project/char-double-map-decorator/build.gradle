dependencies {
    compile project(':trove4j-char-double-map')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: char-double-map-decorator'
                }
            }
        }
    }
}
