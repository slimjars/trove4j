dependencies {
    compile project(':trove4j-custom-object-hash')
    compile project(':trove4j-object-double-map')
    compile project(':trove4j-object-hash-iterator')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: object-double-custom-hash-map'
                }
            }
        }
    }
}
