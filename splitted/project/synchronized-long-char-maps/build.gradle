dependencies {
    compile project(':trove4j-synchronized-long-char-map')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: synchronized-long-char-maps'
                }
            }
        }
    }
}
