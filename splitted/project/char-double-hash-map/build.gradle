dependencies {
    compile project(':trove4j-char-double-hash')
    compile project(':trove4j-char-double-map')
    compile project(':trove4j-hash-primitive-iterator')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: char-double-hash-map'
                }
            }
        }
    }
}
