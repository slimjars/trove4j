dependencies {
    compile project(':trove4j-hash')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: primitive-hash'
                }
            }
        }
    }
}
