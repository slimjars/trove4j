dependencies {
    compile project(':trove4j-byte-double-map')
    compile project(':trove4j-unmodifiable-byte-sets')
    compile project(':trove4j-unmodifiable-double-collections')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: unmodifiable-byte-double-map'
                }
            }
        }
    }
}
