dependencies {
    compile project(':trove4j-unmodifiable-byte-set')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: unmodifiable-byte-sets'
                }
            }
        }
    }
}
