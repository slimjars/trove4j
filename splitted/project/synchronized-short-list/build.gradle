dependencies {
    compile project(':trove4j-short-list')
    compile project(':trove4j-synchronized-short-collection')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: synchronized-short-list'
                }
            }
        }
    }
}
