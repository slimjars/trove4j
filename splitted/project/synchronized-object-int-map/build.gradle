dependencies {
    compile project(':trove4j-object-int-map')
    compile project(':trove4j-synchronized-int-collection')
    compile project(':trove4j-synchronized-set')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: synchronized-object-int-map'
                }
            }
        }
    }
}
