dependencies {
    compile project(':trove4j-long-short-map')
    compile project(':trove4j-synchronized-long-set')
    compile project(':trove4j-synchronized-short-collection')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: synchronized-long-short-map'
                }
            }
        }
    }
}
