dependencies {
    compile project(':trove4j-object-float-map')
    compile project(':trove4j-synchronized-float-collection')
    compile project(':trove4j-synchronized-set')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: synchronized-object-float-map'
                }
            }
        }
    }
}
