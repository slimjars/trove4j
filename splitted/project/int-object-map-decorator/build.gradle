dependencies {
    compile project(':trove4j-int-object-map')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: int-object-map-decorator'
                }
            }
        }
    }
}
