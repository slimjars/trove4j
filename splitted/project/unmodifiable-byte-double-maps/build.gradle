dependencies {
    compile project(':trove4j-unmodifiable-byte-double-map')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: unmodifiable-byte-double-maps'
                }
            }
        }
    }
}
