dependencies {
    compile project(':trove4j-int-double-map')
    compile project(':trove4j-unmodifiable-double-collections')
    compile project(':trove4j-unmodifiable-int-sets')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: unmodifiable-int-double-map'
                }
            }
        }
    }
}
