dependencies {
    compile project(':trove4j-byte-long-map')
    compile project(':trove4j-unmodifiable-byte-sets')
    compile project(':trove4j-unmodifiable-long-collections')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: unmodifiable-byte-long-map'
                }
            }
        }
    }
}
