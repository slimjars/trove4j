dependencies {
    compile project(':trove4j-byte-collection')
    compile project(':trove4j-byte-function')
    compile project(':trove4j-char-byte-iterator')
    compile project(':trove4j-char-byte-procedure')
    compile project(':trove4j-char-set')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: char-byte-map'
                }
            }
        }
    }
}
