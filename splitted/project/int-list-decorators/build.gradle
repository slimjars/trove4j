dependencies {
    compile project(':trove4j-int-list-decorator')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: int-list-decorators'
                }
            }
        }
    }
}
