dependencies {
    compile project(':trove4j-byte-collection')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: byte-queue'
                }
            }
        }
    }
}
