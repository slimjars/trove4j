dependencies {
    compile project(':trove4j-char-int-map')
    compile project(':trove4j-synchronized-char-set')
    compile project(':trove4j-synchronized-int-collection')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: synchronized-char-int-map'
                }
            }
        }
    }
}
