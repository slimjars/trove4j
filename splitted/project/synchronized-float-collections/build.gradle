dependencies {
    compile project(':trove4j-synchronized-float-collection')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: synchronized-float-collections'
                }
            }
        }
    }
}
