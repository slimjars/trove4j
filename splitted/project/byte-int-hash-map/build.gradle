dependencies {
    compile project(':trove4j-byte-int-hash')
    compile project(':trove4j-byte-int-map')
    compile project(':trove4j-hash-primitive-iterator')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: byte-int-hash-map'
                }
            }
        }
    }
}
