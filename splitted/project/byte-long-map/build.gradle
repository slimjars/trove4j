dependencies {
    compile project(':trove4j-byte-long-iterator')
    compile project(':trove4j-byte-long-procedure')
    compile project(':trove4j-byte-set')
    compile project(':trove4j-long-collection')
    compile project(':trove4j-long-function')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: byte-long-map'
                }
            }
        }
    }
}
