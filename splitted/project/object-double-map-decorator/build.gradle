dependencies {
    compile project(':trove4j-object-double-map')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: object-double-map-decorator'
                }
            }
        }
    }
}
