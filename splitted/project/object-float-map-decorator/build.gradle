dependencies {
    compile project(':trove4j-object-float-map')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: object-float-map-decorator'
                }
            }
        }
    }
}
