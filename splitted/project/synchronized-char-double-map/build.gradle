dependencies {
    compile project(':trove4j-char-double-map')
    compile project(':trove4j-synchronized-char-set')
    compile project(':trove4j-synchronized-double-collection')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: synchronized-char-double-map'
                }
            }
        }
    }
}
