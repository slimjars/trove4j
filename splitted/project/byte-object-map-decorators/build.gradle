dependencies {
    compile project(':trove4j-byte-object-map-decorator')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: byte-object-map-decorators'
                }
            }
        }
    }
}
