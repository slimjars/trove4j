dependencies {
    compile project(':trove4j-unmodifiable-double-list')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: unmodifiable-double-lists'
                }
            }
        }
    }
}
