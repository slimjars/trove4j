dependencies {
    compile project(':trove4j-synchronized-char-long-map')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: synchronized-char-long-maps'
                }
            }
        }
    }
}
