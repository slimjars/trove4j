dependencies {
    compile project(':trove4j-synchronized-object-short-map')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: synchronized-object-short-maps'
                }
            }
        }
    }
}
