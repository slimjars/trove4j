dependencies {
    compile project(':trove4j-long-set')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: long-set-decorator'
                }
            }
        }
    }
}
