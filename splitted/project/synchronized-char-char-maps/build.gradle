dependencies {
    compile project(':trove4j-synchronized-char-char-map')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: synchronized-char-char-maps'
                }
            }
        }
    }
}
