dependencies {
    compile project(':trove4j-double-double-iterator')
    compile project(':trove4j-double-double-procedure')
    compile project(':trove4j-double-function')
    compile project(':trove4j-double-set')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: double-double-map'
                }
            }
        }
    }
}
