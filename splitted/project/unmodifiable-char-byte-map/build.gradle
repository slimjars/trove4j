dependencies {
    compile project(':trove4j-char-byte-map')
    compile project(':trove4j-unmodifiable-byte-collections')
    compile project(':trove4j-unmodifiable-char-sets')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: unmodifiable-char-byte-map'
                }
            }
        }
    }
}
