dependencies {
    compile project(':trove4j-short-set')
    compile project(':trove4j-synchronized-short-collection')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: synchronized-short-set'
                }
            }
        }
    }
}
