dependencies {
    compile project(':trove4j-short-double-map')
    compile project(':trove4j-synchronized-double-collection')
    compile project(':trove4j-synchronized-short-set')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: synchronized-short-double-map'
                }
            }
        }
    }
}
