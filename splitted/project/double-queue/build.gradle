dependencies {
    compile project(':trove4j-double-collection')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: double-queue'
                }
            }
        }
    }
}
