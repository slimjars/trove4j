dependencies {
    compile project(':trove4j-synchronized-double-object-map')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: synchronized-double-object-maps'
                }
            }
        }
    }
}
