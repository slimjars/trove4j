dependencies {
    compile project(':trove4j-int-long-map')
    compile project(':trove4j-synchronized-int-set')
    compile project(':trove4j-synchronized-long-collection')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: synchronized-int-long-map'
                }
            }
        }
    }
}
