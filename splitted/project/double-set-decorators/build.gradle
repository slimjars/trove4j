dependencies {
    compile project(':trove4j-double-set-decorator')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: double-set-decorators'
                }
            }
        }
    }
}
