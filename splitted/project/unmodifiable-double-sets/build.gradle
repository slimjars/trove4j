dependencies {
    compile project(':trove4j-unmodifiable-double-set')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: unmodifiable-double-sets'
                }
            }
        }
    }
}
