dependencies {
    compile project(':trove4j-float-char-map')
    compile project(':trove4j-synchronized-char-collection')
    compile project(':trove4j-synchronized-float-set')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: synchronized-float-char-map'
                }
            }
        }
    }
}
