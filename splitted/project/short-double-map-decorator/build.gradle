dependencies {
    compile project(':trove4j-short-double-map')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: short-double-map-decorator'
                }
            }
        }
    }
}
