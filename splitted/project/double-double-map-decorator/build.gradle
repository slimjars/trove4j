dependencies {
    compile project(':trove4j-double-double-map')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: double-double-map-decorator'
                }
            }
        }
    }
}
