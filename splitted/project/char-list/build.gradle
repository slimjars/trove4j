dependencies {
    compile project(':trove4j-char-collection')
    compile project(':trove4j-char-function')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: char-list'
                }
            }
        }
    }
}
