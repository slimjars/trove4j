dependencies {
    compile project(':trove4j-synchronized-byte-long-map')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: synchronized-byte-long-maps'
                }
            }
        }
    }
}
