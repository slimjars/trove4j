dependencies {
    compile project(':trove4j-float-short-hash')
    compile project(':trove4j-float-short-map')
    compile project(':trove4j-hash-primitive-iterator')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: float-short-hash-map'
                }
            }
        }
    }
}
