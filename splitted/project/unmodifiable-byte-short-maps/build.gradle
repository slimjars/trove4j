dependencies {
    compile project(':trove4j-unmodifiable-byte-short-map')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: unmodifiable-byte-short-maps'
                }
            }
        }
    }
}
