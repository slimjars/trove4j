dependencies {
    compile project(':trove4j-float-hash')
    compile project(':trove4j-float-set')
    compile project(':trove4j-hash-primitive-iterator')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: float-hash-set'
                }
            }
        }
    }
}
