dependencies {
    compile project(':trove4j-double-list')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: double-list-decorator'
                }
            }
        }
    }
}
