dependencies {
    compile project(':trove4j-long-int-map')
    compile project(':trove4j-synchronized-int-collection')
    compile project(':trove4j-synchronized-long-set')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: synchronized-long-int-map'
                }
            }
        }
    }
}
