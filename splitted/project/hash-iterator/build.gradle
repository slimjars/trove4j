dependencies {
    compile project(':trove4j-iterator')
    compile project(':trove4j-object-hash')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: hash-iterator'
                }
            }
        }
    }
}
