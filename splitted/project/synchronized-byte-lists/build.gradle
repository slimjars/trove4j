dependencies {
    compile project(':trove4j-synchronized-byte-list')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: synchronized-byte-lists'
                }
            }
        }
    }
}
