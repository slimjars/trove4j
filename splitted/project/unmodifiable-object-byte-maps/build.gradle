dependencies {
    compile project(':trove4j-unmodifiable-object-byte-map')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: unmodifiable-object-byte-maps'
                }
            }
        }
    }
}
