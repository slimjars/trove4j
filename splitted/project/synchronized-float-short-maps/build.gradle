dependencies {
    compile project(':trove4j-synchronized-float-short-map')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: synchronized-float-short-maps'
                }
            }
        }
    }
}
