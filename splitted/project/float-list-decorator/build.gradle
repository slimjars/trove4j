dependencies {
    compile project(':trove4j-float-list')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: float-list-decorator'
                }
            }
        }
    }
}
