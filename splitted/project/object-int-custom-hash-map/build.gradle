dependencies {
    compile project(':trove4j-custom-object-hash')
    compile project(':trove4j-object-hash-iterator')
    compile project(':trove4j-object-int-map')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: object-int-custom-hash-map'
                }
            }
        }
    }
}
