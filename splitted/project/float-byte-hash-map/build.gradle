dependencies {
    compile project(':trove4j-float-byte-hash')
    compile project(':trove4j-float-byte-map')
    compile project(':trove4j-hash-primitive-iterator')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: float-byte-hash-map'
                }
            }
        }
    }
}
