dependencies {
    compile project(':trove4j-float-long-map')
    compile project(':trove4j-synchronized-float-set')
    compile project(':trove4j-synchronized-long-collection')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: synchronized-float-long-map'
                }
            }
        }
    }
}
