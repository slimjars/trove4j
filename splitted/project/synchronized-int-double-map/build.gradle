dependencies {
    compile project(':trove4j-int-double-map')
    compile project(':trove4j-synchronized-double-collection')
    compile project(':trove4j-synchronized-int-set')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: synchronized-int-double-map'
                }
            }
        }
    }
}
