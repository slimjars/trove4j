dependencies {
    compile project(':trove4j-float-list-decorator')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: float-list-decorators'
                }
            }
        }
    }
}
