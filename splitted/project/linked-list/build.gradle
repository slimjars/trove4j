dependencies {
    compile project(':trove4j-linkable')
    compile project(':trove4j-object-procedure')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: linked-list'
                }
            }
        }
    }
}
