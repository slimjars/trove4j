dependencies {
    compile project(':trove4j-unmodifiable-byte-object-map')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: unmodifiable-byte-object-maps'
                }
            }
        }
    }
}
