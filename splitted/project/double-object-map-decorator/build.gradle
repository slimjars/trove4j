dependencies {
    compile project(':trove4j-double-object-map')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: double-object-map-decorator'
                }
            }
        }
    }
}
