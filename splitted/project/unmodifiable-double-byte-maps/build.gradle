dependencies {
    compile project(':trove4j-unmodifiable-double-byte-map')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: unmodifiable-double-byte-maps'
                }
            }
        }
    }
}
