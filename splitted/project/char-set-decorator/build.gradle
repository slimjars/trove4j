dependencies {
    compile project(':trove4j-char-set')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: char-set-decorator'
                }
            }
        }
    }
}
