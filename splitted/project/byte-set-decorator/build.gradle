dependencies {
    compile project(':trove4j-byte-set')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: byte-set-decorator'
                }
            }
        }
    }
}
