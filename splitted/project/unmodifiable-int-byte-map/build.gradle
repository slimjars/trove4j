dependencies {
    compile project(':trove4j-int-byte-map')
    compile project(':trove4j-unmodifiable-byte-collections')
    compile project(':trove4j-unmodifiable-int-sets')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: unmodifiable-int-byte-map'
                }
            }
        }
    }
}
