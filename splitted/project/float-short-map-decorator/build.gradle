dependencies {
    compile project(':trove4j-float-short-map')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: float-short-map-decorator'
                }
            }
        }
    }
}
