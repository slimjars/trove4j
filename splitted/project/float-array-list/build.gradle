dependencies {
    compile project(':trove4j-constants')
    compile project(':trove4j-float-list')
    compile project(':trove4j-hash-functions')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: float-array-list'
                }
            }
        }
    }
}
