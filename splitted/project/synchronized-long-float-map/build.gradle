dependencies {
    compile project(':trove4j-long-float-map')
    compile project(':trove4j-synchronized-float-collection')
    compile project(':trove4j-synchronized-long-set')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: synchronized-long-float-map'
                }
            }
        }
    }
}
