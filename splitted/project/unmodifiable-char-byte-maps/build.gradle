dependencies {
    compile project(':trove4j-unmodifiable-char-byte-map')
}

afterEvaluate { project ->
    uploadArchives {
        repositories {
            mavenDeployer {
                pom.project {
                    name 'Trove4j Slim: unmodifiable-char-byte-maps'
                }
            }
        }
    }
}
